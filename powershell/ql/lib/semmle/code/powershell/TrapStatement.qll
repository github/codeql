import powershell
class TrapStatement extends @trap_statement instanceof Statement
{
    SourceLocation getLocation(){
        trap_statement_location(this, result)
    }
    string toString(){
        result = "TrapStatement at: " + this.getLocation().toString()
    }
}