import powershell
class ParenExpression extends @paren_expression instanceof Expression
{
    PipelineBase getExpression()
    {
        paren_expression(this, result)
    }
    SourceLocation getLocation(){
        paren_expression_location(this, result)
    }
    string toString()
    {
        none()
    }
}