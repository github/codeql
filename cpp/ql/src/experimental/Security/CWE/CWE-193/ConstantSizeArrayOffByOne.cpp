#define MAX_SIZE 1024

struct FixedArray {
  int buf[MAX_SIZE];
};

int main(){
  FixedArray arr;

  for(int i = 0; i <= MAX_SIZE; i++) {
    arr[i] = 0
  }

  for(int i = 0; i < MAX_SIZE; i++) {
    arr[i = 0]
  }
}