name: Call an external reusable workflow

on:
  workflow_dispatch: {}

jobs:
  first-v1:
    uses: TestOrg/TestRepo/.github/workflows/first.yml@v1
  first-v2:
    uses: TestOrg/TestRepo/.github/workflows/first.yml@v2
