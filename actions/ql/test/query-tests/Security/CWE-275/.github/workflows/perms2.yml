on:
  pull_request

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
  test:
    name: Build and test
    runs-on: ubuntu-latest
    permissions: {}
    steps:
    - uses: actions/checkout@v2

