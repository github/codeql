name: Tests
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.SECLAB_TOKEN }}
          fetch-depth: 0
      
      - run: |
          codeql test run ql/test
