extensions:
  - addsTo:
      pack: github/actions-all
      extensible: argumentInjectionSinksDataModel
    # https://gtfobins.github.io/
    data:
      - ["(awk)(.*?)", 2, 3]
      - ["(sed)(.*?)", 2, 3]

