extensions:
  - addsTo:
      pack: codeql/actions-all
      extensible: actionsSummaryModel
    data:
      - ["elastic/apm-server", "*", "input.version", "output.release-version", "taint", "manual"]
      - ["elastic/apm-server", "*", "input.version", "output.release-branch", "taint", "manual"]
