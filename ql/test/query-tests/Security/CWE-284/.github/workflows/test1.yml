name: test

on:
  pull_request:

jobs:
  test1:
    runs-on: [self-hosted, X64, Linux, 16c32g]
    steps:
      - run: cmd
  test2:
    runs-on:
      group: my-group
      labels: [self-hosted, label-1]
    steps:
      - run: cmd
  test3:
    runs-on:
      - 'self-hosted'
      - 'linux'
      - 'x64'
      - 'metal'
    steps:
      - run: echo "foo"
  test4:
    runs-on: self-hosted-azure
    steps:
    - run: cmd
