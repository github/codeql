/**
 * @id py/examples/elif
 * @name Elif statement
 * @description Finds `elif` sub-statements within `if` statements
 * @tags if
 *       else
 */

import python

from If i
where i.isElif()
select i
