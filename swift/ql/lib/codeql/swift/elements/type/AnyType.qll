/**
 * Provides a class representing the Swift `Any` type, which is a supertype of all other types.
 */
private import swift

/*
 * The Swift `Any` type, which is a supertype of all other types.
 */
class AnyType extends ExistentialType {
  AnyType() {
    this.getFullName() = "Any"
  }
}
