// generated by codegen/codegen.py, do not edit
/**
 * This module provides the generated definition of `DotSyntaxBaseIgnoredExpr`.
 * INTERNAL: Do not import directly.
 */

private import codeql.swift.generated.Synth
private import codeql.swift.generated.Raw
import codeql.swift.elements.expr.Expr
import codeql.swift.elements.expr.internal.ExprImpl::Impl as ExprImpl

/**
 * INTERNAL: This module contains the fully generated definition of `DotSyntaxBaseIgnoredExpr` and should not
 * be referenced directly.
 */
module Generated {
  /**
   * INTERNAL: Do not reference the `Generated::DotSyntaxBaseIgnoredExpr` class directly.
   * Use the subclass `DotSyntaxBaseIgnoredExpr`, where the following predicates are available.
   */
  class DotSyntaxBaseIgnoredExpr extends Synth::TDotSyntaxBaseIgnoredExpr, ExprImpl::Expr {
    override string getAPrimaryQlClass() { result = "DotSyntaxBaseIgnoredExpr" }

    /**
     * Gets the qualifier of this dot syntax base ignored expression.
     *
     * This includes nodes from the "hidden" AST. It can be overridden in subclasses to change the
     * behavior of both the `Immediate` and non-`Immediate` versions.
     */
    Expr getImmediateQualifier() {
      result =
        Synth::convertExprFromRaw(Synth::convertDotSyntaxBaseIgnoredExprToRaw(this)
              .(Raw::DotSyntaxBaseIgnoredExpr)
              .getQualifier())
    }

    /**
     * Gets the qualifier of this dot syntax base ignored expression.
     */
    final Expr getQualifier() {
      exists(Expr immediate |
        immediate = this.getImmediateQualifier() and
        if exists(this.getResolveStep()) then result = immediate else result = immediate.resolve()
      )
    }

    /**
     * Gets the sub expression of this dot syntax base ignored expression.
     *
     * This includes nodes from the "hidden" AST. It can be overridden in subclasses to change the
     * behavior of both the `Immediate` and non-`Immediate` versions.
     */
    Expr getImmediateSubExpr() {
      result =
        Synth::convertExprFromRaw(Synth::convertDotSyntaxBaseIgnoredExprToRaw(this)
              .(Raw::DotSyntaxBaseIgnoredExpr)
              .getSubExpr())
    }

    /**
     * Gets the sub expression of this dot syntax base ignored expression.
     */
    final Expr getSubExpr() {
      exists(Expr immediate |
        immediate = this.getImmediateSubExpr() and
        if exists(this.getResolveStep()) then result = immediate else result = immediate.resolve()
      )
    }
  }
}
