// generated by codegen/codegen.py
import codeql.swift.elements.expr.Expr
import codeql.swift.elements.type.TypeRepr

class KeyPathExprBase extends @key_path_expr, Expr {
  override string getAPrimaryQlClass() { result = "KeyPathExpr" }

  TypeRepr getRoot() {
    exists(TypeRepr x |
      key_path_expr_roots(this, x) and
      result = x.resolve()
    )
  }

  predicate hasRoot() { exists(getRoot()) }

  Expr getParsedPath() {
    exists(Expr x |
      key_path_expr_parsed_paths(this, x) and
      result = x.resolve()
    )
  }

  predicate hasParsedPath() { exists(getParsedPath()) }
}
