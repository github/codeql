// generated by codegen/codegen.py, do not edit
/**
 * This module provides the generated definition of `KeyPathApplicationExpr`.
 * INTERNAL: Do not import directly.
 */

private import codeql.swift.generated.Synth
private import codeql.swift.generated.Raw
import codeql.swift.elements.expr.Expr
import codeql.swift.elements.expr.internal.ExprImpl::Impl as ExprImpl

/**
 * INTERNAL: This module contains the fully generated definition of `KeyPathApplicationExpr` and should not
 * be referenced directly.
 */
module Generated {
  /**
   * INTERNAL: Do not reference the `Generated::KeyPathApplicationExpr` class directly.
   * Use the subclass `KeyPathApplicationExpr`, where the following predicates are available.
   */
  class KeyPathApplicationExpr extends Synth::TKeyPathApplicationExpr, ExprImpl::Expr {
    override string getAPrimaryQlClass() { result = "KeyPathApplicationExpr" }

    /**
     * Gets the base of this key path application expression.
     *
     * This includes nodes from the "hidden" AST. It can be overridden in subclasses to change the
     * behavior of both the `Immediate` and non-`Immediate` versions.
     */
    Expr getImmediateBase() {
      result =
        Synth::convertExprFromRaw(Synth::convertKeyPathApplicationExprToRaw(this)
              .(Raw::KeyPathApplicationExpr)
              .getBase())
    }

    /**
     * Gets the base of this key path application expression.
     */
    final Expr getBase() {
      exists(Expr immediate |
        immediate = this.getImmediateBase() and
        if exists(this.getResolveStep()) then result = immediate else result = immediate.resolve()
      )
    }

    /**
     * Gets the key path of this key path application expression.
     *
     * This includes nodes from the "hidden" AST. It can be overridden in subclasses to change the
     * behavior of both the `Immediate` and non-`Immediate` versions.
     */
    Expr getImmediateKeyPath() {
      result =
        Synth::convertExprFromRaw(Synth::convertKeyPathApplicationExprToRaw(this)
              .(Raw::KeyPathApplicationExpr)
              .getKeyPath())
    }

    /**
     * Gets the key path of this key path application expression.
     */
    final Expr getKeyPath() {
      exists(Expr immediate |
        immediate = this.getImmediateKeyPath() and
        if exists(this.getResolveStep()) then result = immediate else result = immediate.resolve()
      )
    }
  }
}
