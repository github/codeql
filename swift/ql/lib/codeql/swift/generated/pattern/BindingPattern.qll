// generated by codegen/codegen.py, do not edit
/**
 * This module provides the generated definition of `BindingPattern`.
 * INTERNAL: Do not import directly.
 */

private import codeql.swift.generated.Synth
private import codeql.swift.generated.Raw
import codeql.swift.elements.pattern.Pattern
import codeql.swift.elements.pattern.internal.PatternImpl::Impl as PatternImpl

/**
 * INTERNAL: This module contains the fully generated definition of `BindingPattern` and should not
 * be referenced directly.
 */
module Generated {
  /**
   * INTERNAL: Do not reference the `Generated::BindingPattern` class directly.
   * Use the subclass `BindingPattern`, where the following predicates are available.
   */
  class BindingPattern extends Synth::TBindingPattern, PatternImpl::Pattern {
    override string getAPrimaryQlClass() { result = "BindingPattern" }

    /**
     * Gets the sub pattern of this binding pattern.
     *
     * This includes nodes from the "hidden" AST. It can be overridden in subclasses to change the
     * behavior of both the `Immediate` and non-`Immediate` versions.
     */
    Pattern getImmediateSubPattern() {
      result =
        Synth::convertPatternFromRaw(Synth::convertBindingPatternToRaw(this)
              .(Raw::BindingPattern)
              .getSubPattern())
    }

    /**
     * Gets the sub pattern of this binding pattern.
     */
    final Pattern getSubPattern() {
      exists(Pattern immediate |
        immediate = this.getImmediateSubPattern() and
        if exists(this.getResolveStep()) then result = immediate else result = immediate.resolve()
      )
    }
  }
}
