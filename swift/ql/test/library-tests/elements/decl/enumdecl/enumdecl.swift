
enum MyColours {
	case red
	case green, yellow
	case blue
}

enum MyContainer {
	case str(String)
	case pair(x: Int, y: Int)
}

enum MyNumbers: Int {
	case one = 1, two
	case three, four
}

enum MyGreek {
	case alpha, beta, gamma, delta, epsilon
}
