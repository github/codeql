cc_library(
    name = "log",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    visibility = ["//visibility:public"],
    deps = [
        "@absl//absl/strings",
        "@binlog",
        "@json",
    ],
)
