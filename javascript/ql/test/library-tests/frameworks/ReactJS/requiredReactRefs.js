var React = require("react");

React;

function f(){
    React;
}

React.Children.map(x, y, z);

function g() {
    var React = require("react");

    React;

    function h(){
        React;
    }

    React.Children.map(x, y, z);
}
