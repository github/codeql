
filter => {
    filter.path
}

function originalPositionIn(trackedSource, line, column, token, allowNFallbacks) {
    trackedSource.path
}

function renderSinglePlugin( context, siteUrl ) {
    context.params.plugin
}

class Page extends React.Component {

    componentWillReceiveProps(newProps, b)  {
        newProps.params.uid
    }

}

function f(prop, propHash) {
    prop.method;
    propHash[name];
};
