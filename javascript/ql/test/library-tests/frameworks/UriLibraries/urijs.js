var urijs = require("urijs");
var URIjs = require("URIjs");

new URI();

new urijs();

new URIjs();

URI();

var u = new URI(x);

u = u.username(x);

u = u
    .username(x)
    .tld(x);

var r = u.normalizeQuery();
