on: issue_comment

jobs:
  echo-chamber:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo '${{ github.event.comment.body }}' // $ TODO-SPURIOUS: Alert

  echo-chamber2:
    runs-on: ubuntu-latest
    steps:
    - run: echo '${{ github.event.comment.body }}' // $ TODO-SPURIOUS: Alert
    - run: echo '${{ github.event.issue.body }}' // $ TODO-SPURIOUS: Alert
    - run: echo '${{ github.event.issue.title }}' // $ TODO-SPURIOUS: Alert

  echo-chamber3:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/github-script@v3
      with:
        script: console.log('${{ github.event.comment.body }}') // $ TODO-SPURIOUS: Alert
    - uses: actions/github-script@v3
      with:
        script: console.log('${{ github.event.issue.body }}') // $ TODO-SPURIOUS: Alert
    - uses: actions/github-script@v3
      with:
        script: console.log('${{ github.event.issue.title }}') // $ TODO-SPURIOUS: Alert