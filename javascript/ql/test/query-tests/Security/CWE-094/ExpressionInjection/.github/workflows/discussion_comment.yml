on: discussion_comment

jobs:
  echo-chamber:
    runs-on: ubuntu-latest
    steps:
    - run: echo '${{ github.event.discussion.title }}' // $ TODO-SPURIOUS: Alert
    - run: echo '${{ github.event.discussion.body }}' // $ TODO-SPURIOUS: Alert
    - run: echo '${{ github.event.comment.body }}' // $ TODO-SPURIOUS: Alert