function distanceFromOrigin(point) {
    var [x, x] = point; // $ Alert
    return Math.sqrt(x*x + y*y);
}

var { x: x, y: x } = o; // $ Alert

var { x, x } = o; // $ Alert


var { x: x, x: y } = o;


var { p = x, q = x } = o;
