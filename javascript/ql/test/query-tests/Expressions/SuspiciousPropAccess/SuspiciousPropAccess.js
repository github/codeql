function f() {
  var result;
  res = computeInterestingResult();
  return result.value; // $ TODO-SPURIOUS: Alert
}

(function(_window) {
  if (_window)
    setupNode();

  function parse() {
    return uuid;
  }

  var uuid = parse;
  uuid.v1 = v1;
})();
