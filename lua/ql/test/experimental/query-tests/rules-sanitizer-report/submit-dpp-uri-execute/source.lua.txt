luci = { http = {} }

function luci.http.formvalue(name)
  return name
end

os = {
  execute = function(cmd)
    return cmd
  end
}

function submit_dpp_uri()
  local dpp_uri = luci.http.formvalue("dpp_uri")
  os.execute(dpp_uri)
end

submit_dpp_uri()
